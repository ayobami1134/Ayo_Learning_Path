#include <stdio.h>

// no return value with no arguments
void displayNum() {
    // This function does not return any value and takes no arguments
    printf("Number: %d\n", 100);
}

int main() {
    // Function call
    displayNum(); // No return value
    return 0;
}
