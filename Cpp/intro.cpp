#include <iostream>  // Include the Input Output stream library for input and output functions

int main() {  // Define the main function, the entry point of the program
    std::cout << "Hello, World!" << std::endl;  // Print the string "Hello, World!" followed by a newline character to the console
    return 0;  // Return 0 to indicate that the program executed successfully
}